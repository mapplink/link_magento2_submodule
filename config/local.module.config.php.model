<?php

return array(
    'default_stores'=>array(<store using default data>, ...),
    'store_limits'=>array(
        <store id>=><last order increment id>
    ),
    'store_currencies'=>array(
        <store id: 0 for default>=>array(<currency identifier>, ...)
    ),
    'store_map'=>array(
         <store id> => array(<entity type> => array(<magento2 code> => <magelink code>, ...))
         <-store id> => array(<entity type> => array(<magento2 code> => <magento2 code>_default, ...)) // for write and clean up purpose only
    )
);
